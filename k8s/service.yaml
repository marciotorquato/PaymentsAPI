apiVersion: v1
kind: Service
metadata:
  name: payments-api-service
spec:
  selector:
    app: payments-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30003
  type: NodePort